{
  "name": "client-cache-query",
  "version": "0.0.2",
  "description": "client-cache is a React library that applies React Query's powerful caching mechanism to client-side state management. It enables persistent local state across page reloads",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "lint": "eslint",
    "format": "prettier --w 'src/**/*.{ts,tsx,js,jsx,json,css,md}'",
    "pub": "pnpm run build && npm publish"
  },
  "keywords": [],
  "author": "joseph0926",
  "license": "MIT",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "dependencies": {
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.11",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
